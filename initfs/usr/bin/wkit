#!/bin/sh
#

if [ -d /var/config ]; then
    chmod +x / `grep '^#!/bin' -rl /var/config`
    cp -a /var/config/* /etc/
fi

if [ -f /usr/wkit/bundles/$1 ]; then
    /usr/wkit/bundles/$@
    exit $?
fi

/usr/wkit/service "$@"
