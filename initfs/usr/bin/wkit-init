#!/bin/sh
#

if [ -d /var/config ]; then
    chmod +x / `grep -rl '^#!/bin' /var/config`
    cp -a /var/config/* /etc/
fi

if [ -d /etc/init.d ]; then
    rm -f `grep -rl openrc /etc/init.d`
    run-parts -a start /etc/init.d
fi

echo "well done !"
