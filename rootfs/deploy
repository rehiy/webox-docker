#!/bin/sh
#

set -xe

chmod +x /usr/bin/wkit*
chmod +x `grep '^#!/bin' -rl /usr/wkit`

for NAME in mysql nginx php7 redis; do
    install=/usr/wkit/ports/$NAME-install
    $install && rm -f $install
done

rm -rf /deploy
