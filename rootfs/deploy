#!/bin/sh -e
#

chmod +x `grep '^#!/bin' -rl /bld`

cp -a /bld/usr /

for NAME in $WBX_APPS; do
    install=/usr/wkit/bundles/$NAME-add
    $install && rm -f $install
done

cp -a /bld/etc /
cp -a /bld/var /

rm -r /bld
