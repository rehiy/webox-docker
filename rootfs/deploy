#!/bin/sh -e
#

chmod +x `grep '^#!/bin' -rl /deploy`

cp -a /deploy/usr /

for NAME in $WBX_APPS; do
    install=/usr/wkit/bundles/$NAME-add
    $install && rm -f $install
done

cp -a /deploy/etc /
cp -a /deploy/var /

rm -r /deploy
